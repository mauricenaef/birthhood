import { CardFilterPipe } from './card-filter.pipe';

describe('CardFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CardFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
